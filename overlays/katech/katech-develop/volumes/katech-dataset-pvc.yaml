apiVersion: v1
kind : PersistentVolumeClaim
metadata:
  name: katech-dataset-pvc
spec:
  storageClassName: katech-dataset-dev
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100G
