apiVersion: apps/v1
kind: Deployment
metadata:
  name: uyuni-manager-websocket
spec:
  selector:
    matchLabels:
      app: uyuni-manager-websocket
  template:
    metadata:
      labels:
        app: uyuni-manager-websocket
    spec:
      containers:
      - name: uyuni-manager-websocket
        image: harbor.xiilab.com/uyuni-suite/uyuni-manager-websocket:latest
        ports:
        - name: http
          containerPort: 8080
          protocol: TCP
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
        volumeMounts:
        - name: kube-config
          mountPath: "/root/.kube"
          readOnly: true
      
      imagePullSecrets:
      - name: regcred
      volumes:
      - name: kube-config
        secret:
          secretName: kube-config
