apiVersion: v1
kind : PersistentVolumeClaim
metadata:
  name: uyuni-pvc
  namespace: uyuni
spec:
  storageClassName: uyuni-project
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1G
